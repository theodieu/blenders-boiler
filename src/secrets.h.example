#pragma once
// Copy this file to secrets.h (same directory) and set your own values.
// secrets.h is gitignored; without it the firmware builds with the public
// placeholder credentials defined in main.cpp.

#define REMOTEXY_WIFI_SSID        "your-ap-name"
#define REMOTEXY_WIFI_PASSWORD    "your-wifi-password"    // WPA2: min 8 chars
#define REMOTEXY_ACCESS_PASSWORD  "your-app-password"
